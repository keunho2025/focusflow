# FocusFlow 개발 기록 - 2026-05-18

## 📌 핵심 작업: 아날로그 시계 타이머 구현

### 🎯 목표
Pomodoro 타이머의 디지털 시간 표시를 아날로그 시계로 변경하여 시각적 진행도 표시

---

## ✅ 완료된 작업

### 1️⃣ Next.js 버전 (src/app/page.tsx) 구현
- **아날로그 시계 SVG 생성**
  - 흰색 원형 시계 얼굴 (반지름 95px)
  - 12개 시간 표시 (검은색 선)
  - 파란 분침 (굵기 6px, 60px 길이)
  - 빨간 초침 (굵기 2px, 70px 길이)
  - 중앙 검은 점

- **손 회전 계산**
  ```javascript
  // 분침: 세션 경과시간 기반 (0-360도)
  const minuteAngle = (elapsedSeconds / totalSeconds) * 360 - 90
  
  // 초침: 남은 초 기반 (0-360도)
  const secondAngle = (seconds / 60) * 360 - 90
  ```

- **마운트 체크로 Hydration 오류 해결**
  - 동적 SVG 계산을 `mounted` 조건으로 래핑
  - 서버/클라이언트 부동소수점 차이 제거

### 2️⃣ HTML 버전 (index.html) 구현
- **아날로그 시계 SVG**
  - 12개 시간 표시 (SVG transform으로 30도씩 회전)
  - 분침 & 초침 초기 위치 설정
  - 중앙 시간 표시 오버레이

- **JavaScript 업데이트 함수**
  ```javascript
  function updateAnalogClock() {
    // 분침과 초침 회전 계산
    const minuteRotation = (secondsElapsed / totalSeconds) * 360
    const secondRotation = (seconds / 60) * 360
    
    // SVG transform 속성으로 회전 적용
    minuteHand.setAttribute('transform', `rotate(${minuteRotation} 100 100)`)
    secondHand.setAttribute('transform', `rotate(${secondRotation} 100 100)`)
  }
  ```

### 3️⃣ 아날로그 시계 손 회전 오류 수정
**문제:** HTML 버전의 CSS `transform-origin` 방식이 SVG 스케일링으로 인해 회전 중심 불일치
- SVG viewBox (0 0 200 200) → 표시 크기 160px × 160px
- CSS 픽셀 좌표계와 SVG 좌표계 불일치

**해결책:** CSS transform → SVG `transform` 속성으로 변경
- `setAttribute('transform', 'rotate(angle cx cy)')` 사용
- SVG 좌표계 (100, 100) 기준 정확한 회전

### 4️⃣ 개발 서버 포트 변경
- localhost:3001 → localhost:3000으로 변경
- 기존 프로세스 정리 후 재시작

### 5️⃣ Git 커밋 및 푸시
**3개 커밋 생성:**

| 커밋 해시 | 제목 | 내용 |
|---------|------|------|
| `21375e5` | Implement analog clock timer visualization | 아날로그 시계 기본 구현 |
| `d9ff644` | Fix analog clock hand rotation in HTML version | SVG transform 수정 |
| `8ad9675` | Update project documentation and CSS configuration | CLAUDE.md & globals.css 업데이트 |

**GitHub 푸시 완료:**
- 🔗 https://github.com/keunho2025/focusflow

---

## 🔧 기술 상세

### 아날로그 시계 작동 원리

#### 분침 (Minute Hand)
```
회전각 = (경과시간 / 전체시간) × 360도
- 세션 시작: 0도 (12시)
- 세션 중반: 180도 (6시)
- 세션 종료: 360도 (12시)
```

#### 초침 (Second Hand)
```
회전각 = (남은초 % 60 / 60) × 360도
- 매초마다 6도씩 회전
- 부드러운 초 표시
```

### 뷰 구성 (Home 화면)
```
┌─────────────────────────────┐
│         Dashboard            │
├──────────────┬──────────────┤
│ 왼쪽 열      │  오른쪽 열    │
│              │              │
│ • Today's    │ • 아날로그    │
│   Progress   │   시계        │
│   (세션/시간)│              │
│              │ • 현재 할일   │
│ • By         │              │
│   Category   │ • 시작/리셋   │
│   (분류별)   │   버튼        │
│              │              │
│ • Completed  │ • 모드 선택   │
│   (완료목록) │   (Focus/    │
│              │    Break/    │
│              │    Long)     │
└──────────────┴──────────────┘
```

---

## 📊 변경 파일 통계

| 파일 | 추가 | 제거 | 상태 |
|------|------|------|------|
| src/app/page.tsx | 1081 | 59 | ✅ 수정 |
| index.html | 1401 | - | ✅ 신규 |
| CLAUDE.md | 30 | 1 | ✅ 수정 |
| src/app/globals.css | - | - | ✅ 수정 |

**총 변경:** 2512 라인

---

## ✨ 주요 특징

### ✅ 구현된 기능
- ✅ 아날로그 시계 시각화
- ✅ 실시간 분침/초침 회전
- ✅ 모드별 타이머 (Focus 25분, Break 5분, Long 45분)
- ✅ 중앙 시간 표시 (MM:SS)
- ✅ 동적 모드 레이블
- ✅ 부드러운 CSS 애니메이션
- ✅ 반응형 레이아웃 (2열 대시보드)

### 🎨 디자인
- 파란색 분침 (#3fb8d4)
- 빨간색 초침 (#ff6b6b 또는 #ef4444)
- 흰색 시계 얼굴
- 검은색 시간 표시

---

## 🧪 테스트 항목

### Next.js 버전 (localhost:3000)
- [ ] 페이지 로드 확인
- [ ] 2열 대시보드 레이아웃
- [ ] 12시간 표시 선 확인
- [ ] 파란 분침/빨간 초침 표시
- [ ] 타이머 시작 → 분침 회전
- [ ] 초침 1초씩 회전
- [ ] 모드 변경 (Focus/Break/Long)
- [ ] 시간 표시 업데이트

### HTML 버전 (localhost:8000/index.html)
- [ ] 위와 동일한 테스트

---

## 📝 학습 포인트

### Hydration Mismatch 해결
SVG와 수학 계산이 포함된 컴포넌트는 서버/클라이언트 부동소수점 차이로 hydration 오류 발생
→ `mounted` 체크로 클라이언트 렌더링만 수행

### SVG vs CSS Transform
- **CSS transform**: 뷰포트 좌표계, 스케일링 영향
- **SVG transform**: SVG 좌표계, 독립적이고 정확함
→ SVG 요소는 SVG transform 속성 사용 권장

### 좌표계 변환
- viewBox 스케일링 시 CSS 픽셀 계산 필요
- SVG 좌표 (100, 100) ≠ CSS 픽셀 (80, 80) at 0.8x scale

---

## 🚀 다음 단계 (선택 사항)

- [ ] 타이머 알림음 추가
- [ ] 브라우저 알림 기능
- [ ] 세션 통계 차트
- [ ] 다크모드 지원
- [ ] 모바일 최적화

---

## 📦 배포 정보

**Repository:** https://github.com/keunho2025/focusflow  
**Branch:** master  
**최근 커밋:** 8ad9675  
**Dev Server:** http://localhost:3000  
**HTML Version:** http://localhost:8000/index.html

---

**작성일:** 2026-05-18  
**작성자:** Claude Haiku 4.5  
**상태:** ✅ 완료
